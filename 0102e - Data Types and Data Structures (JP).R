#===============================================================================
# 1. Create a numeric vector, v_odd, containing odd numbers between 1 to 20.
#===============================================================================
v_odd <- seq(1, 20, 2)
v_odd

#===============================================================================
# 2. Create an integer vector, v_even, containing even numbers between
#    50 to 100.
#===============================================================================
v_even <- seq(50, 100, by = 2)
v_even

#===============================================================================
# 3. Add the vectors v_odd and v_even and name it v; then get the sum of v.
#    What did you observe?
#===============================================================================
v <- v_odd + v_even
v
sum(v)

#===============================================================================
# 4. Create v2 from v by replacing the first element with 1000 and the last
#    element with 2000. What is the new sum of v2?
#===============================================================================
v2 <- v
v2[1] <- 1000
v2[length(v2)] <- 2000
sum(v2)


#===============================================================================
# 5. Filter v2 to get only the elements that are greater than 100 and name it
#    v3. What is the length of v3?
#===============================================================================
v3 <- v2[v2 > 100]
length(v3)


#===============================================================================
# 6. What is the length of v4?
#===============================================================================
v4 <- c(rep(2, 20), NA_real_, rep(3, 4), 5:200 * 6, rep(NA_real_, 4))
length(v4)


#===============================================================================
# 7. How many NA values are in v4?
#===============================================================================
sum(is.na(v4))


#===============================================================================
# 8. What is the mean of v4?
#===============================================================================
mean(v4, na.rm = TRUE)
mean(v4)

#===============================================================================
# 9. Remove all NA values in v4 and name it v5.
#===============================================================================
v5 <- v4[!is.na(v4)]
v5

#===============================================================================
# 10. Replace all NA values in v4 with the mean of v4 (calculated in item 8)
#     and name it v6.
#===============================================================================

m <- mean(v4, na.rm = TRUE)
v6 <- v4
v6[is.na(v6)] <- m
v6


#===============================================================================
# 11. Get the unique values in v7.
#===============================================================================

v7 <- c(
  "F", "O", "G", "V", "G", "O", "J", "S", "E", "Y", "S",
  "K", "Y", "E", "X", "H", "Z", "M", "U", "U", "Y", "J"
)

unique(v7)



#===============================================================================
# 12. Filter v7 to get only the elements that are not "X", "Y", "Z".
#===============================================================================
v7[v7 != "X" & v7 != "Y" & v7 != "Z"]


#===============================================================================
# 13. Filter v7 to get only the elements that are "X", "Y", "Z".
#===============================================================================
v7[v7 == "X" | v7 == "Y" | v7 == "Z"]


#===============================================================================
# 14. Find the indices of elements that are "X", "Y", "Z".
#===============================================================================
which(v7 == "X" | v7 == "Y" | v7 == "Z")


#===============================================================================
# 15. Sort v7 in ascending order.
#===============================================================================
sort(v7)


#===============================================================================
# 16. Create a table of counts for each unique value in v7.
#===============================================================================
table(v7)


#===============================================================================
# 17. Create a list named person with the following elements:
#       name: “Mark”
#       age: 28
#       hobbies: A vector containing “reading”, “traveling”, and “swimming”
#===============================================================================
person <- list(
  name = "Mark",
  age = 28,
  hobbies = c("reading", "traveling", "swimming")
)


#===============================================================================
# 18. Access the age element from the person list.
#===============================================================================
person$age


#===============================================================================
# 19. Add a new element address to the person list with the value “123 Main St”.
#===============================================================================
person$address <- "123 Main St"


#===============================================================================
# 20. Access the hobbies element from the person list and retrieve the second
#     hobby.
#===============================================================================
person$hobbies[2]


#===============================================================================
# 21. Select the columns name, height, and mass.
#===============================================================================
library(dplyr)
starwars[, c("name", "height", "mass")]


#===============================================================================
# 22. Filter the rows where height is greater than 200.
#===============================================================================
starwars[starwars$height > 200, ]



#===============================================================================
# 23. Arrange the data in descending order of mass
#===============================================================================
starwars[order(starwars$mass, decreasing = TRUE), ]

#===============================================================================
# 24. Create a new column bmi (Body Mass Index) using the formula:
#     bmi = mass / (height/100)^2.
#===============================================================================
starwars$bmi <- starwars$mass / (starwars$height/100)^2



#===============================================================================
# 25. Filter the rows where bmi is greater than 25.
#===============================================================================
starwars[starwars$bmi > 25, ]

